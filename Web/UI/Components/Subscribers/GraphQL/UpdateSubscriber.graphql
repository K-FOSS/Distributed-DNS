mutation UpdateSubscriber($subscriberId: ID!, $input: UpdateSubscriberInput!) {
  updateSubscriber(subscriberId: $subscriberId, input: $input) {
    id
    name
    subscribedZones {
      domainName
      id
    }
  }
}
