mutation DeleteResourceRecord($resourceRecordId: ID!) {
  deleteResourceRecord(resourceRecordId: $resourceRecordId) {
    id
    domainName
    resourceRecords {
      id
      host
      ttl
      data
    }
  }
}
