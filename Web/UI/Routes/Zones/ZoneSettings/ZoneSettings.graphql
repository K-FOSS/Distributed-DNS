query ZoneSettings($zoneId: String!) {
  zone(zoneId: $zoneId) {
    id
    domainName

    accessPermissions {
      id
      
      user {
      	id
        
        username
        
      }
      
      accessPermissions
    }

    zoneSettings {
      id
      contact
    }
  }
}
