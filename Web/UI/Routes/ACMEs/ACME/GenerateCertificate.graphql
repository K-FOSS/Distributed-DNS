mutation GenerateCertificate($acmeId: String!) {
  generateCertificate(acmeId: $acmeId) {
    id

    certificates {
      id

      createdAt
    }
    
    domains {
      id
      
      domains
      
      zone {
        id
        domainName
      }
      
      domains
    }
  }
}
