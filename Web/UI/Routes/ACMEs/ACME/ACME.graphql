query ACME($acmeId: String!) {
  ACME(acmeId: $acmeId) {
    id
    
    name
    
    domains {
      id
      
      domains
      
      zone {
        id
        domainName
      }
      
      domains
    }
    
    contactEmail
  }
}